# Core — the app, pipeline, and tests run with just these
pandas
numpy
scikit-learn
yfinance
feedparser
streamlit
plotly
matplotlib
joblib
xgboost
pytest

# Deep learning sentiment (optional — the app falls back to a built-in
# lexicon engine if these aren't installed)
transformers
torch

# Topological data analysis (optional — statistical fallback is used if
# giotto-tda is unavailable; it can be tricky to build on newer Pythons)
# giotto-tda
